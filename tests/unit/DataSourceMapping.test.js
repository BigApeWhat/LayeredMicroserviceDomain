const chai = require('chai');
const assert = require('assert');

const datasourceOutput = require('../../cryptocurrency/datasource/mapping/CrypoCurrencyOutput');

chai.should();

describe('Schema validation tests', () => {
    it('DamagesInputBody schema', done => {
        assert.equal(true, true);
        done();
    });

});
